class Solution:
    def deleteDuplicates(self, head: Optional[ListNode]) -> Optional[ListNode]
        temp=head
        while temp:
            while temp.next and temp.next.next==temp.val
                temp.next==temp.next.next
            temp=temp.next
        return head